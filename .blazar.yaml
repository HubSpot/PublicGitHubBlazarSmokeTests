containers:
  - name: main
    securityContext: unprivileged
    image: docker.hubteam.com/wailmer/blazar-executor-frontend-ubuntu:855

steps:
  - description: Webhook with correct secret service
    container: main
    commands:
      - echo test
